


SELECT_f(5)                     Scicos Block                     SELECT_f(5)






NAME
  SELECT - Scicos select block

DIALOG PARAMETERS
  Block label         : A string defining the block label. This label is
                      written under the block.
  number of inputs    : a scalar. Number of regular and event inputs.
  initial connected input
                      : a integer. It must be between 1 and the number of
                      inputs.

DESCRIPTION
  The blocks routes one of the regular inputs to the unique regular output.
  the choice of which input is to be routed is done, initially by the "ini-
  tial connected input" parameter. Then, everytime an input event arrives on
  the i-th input event port, the i-th regular input port is routed to the
  regular output.







































