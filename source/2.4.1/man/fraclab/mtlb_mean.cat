
mtlb_mean(2)                   Scilab Function                   mtlb_mean(2)
NAME
  mtlb_mean - rithmetic mean.

  Author: Paulo Goncalves

  Arithmetic mean.
      y = mtlb_mean(x) is the arithmetic mean of the elements of x.

Usage

  y = mtlb_mean(x) ;
Input parameters
       o x : Complex valued vector [1,N]
Output parameters
       o y : Complex scalar Arithmetic mean of the elements of input vector
         x.
See also:
  sum
