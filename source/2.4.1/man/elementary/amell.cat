
amell(G)                       Scilab Function                       amell(G)
NAME
  amell - Jacobi's am function

CALLING SEQUENCE
  [sn]=amell(u,k)

PARAMETERS

  u         : real scalar or vector

  k         : scalar

  sn        : real scalar or vector

DESCRIPTION
  Computes Jacobi's elliptic function am(u,k) where k is the parameter and u
  is the argument. If u is a vector sn is the vector of the (element wise)
  computed values.  Used in function %sn.

SEE ALSO
  delip, %sn, %asn
