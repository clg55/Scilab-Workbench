
fix(1)                         Scilab Function                         fix(1)
NAME
  fix - rounding towards zero

CALLING SEQUENCE
  [y]=fix(x)

PARAMETERS

  x    : a real matrix

  y    : integer matrix

DESCRIPTION
  fix(x) returns an integer matrix made of nearest rounded integers toward
  zero,i.e, y=sign(x).*floor(abs(x)). Same as int.

SEE ALSO
  round, floor, ceil
