
ssprint(1)                     Scilab Function                     ssprint(1)
NAME
  ssprint - pretty print for linear system

CALLING SEQUENCE
  ssprint(sl [,out])

PARAMETERS

  sl        : list (syslin list)

  out       : output (default value out=%io(2))

DESCRIPTION
  pretty print of a linear system in state-space form sl=(A,B,C,D) syslin
  list.

EXAMPLE
   a=[1 1;0 1];b=[0 1;1 0];c=[1,1];d=[3,2];
   ssprint(syslin('c',a,b,c,d))
   ssprint(syslin('d',a,b,c,d))

SEE ALSO
  texprint
