iserror           Scilab Group           Scilab Function            iserror
NAME
   iserror - error test
  
CALLING SEQUENCE
 iserror([n])
DESCRIPTION
   tests if error number n has occurred (after a call to errcatch). iserror
  returns 1 if the error occurred a nd 0 otherwise
  
   n>0 is the error number ; all errors are tested with n<0.
  
SEE ALSO
   error, errcatch  
  
