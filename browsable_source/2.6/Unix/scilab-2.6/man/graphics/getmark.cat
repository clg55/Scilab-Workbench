getmark           Scilab Group           Scilab Function            getmark
NAME
   getmark - dialog to select mark (symbol)
  
CALLING SEQUENCE
 [mark,mrkSize]=getmark()
PARAMETERS
 mark : integer, the number of the selected mark
      
 mrkSize
       : integer, the size of the selected mark
      
DESCRIPTION
   getmark opens a graphic window to select a mark (symbol). Usage:
  [mark,mrkSize]=getmark().  xset("mark size",mrkSize).
  plot2d(x,y,style=mark).
  
SEE ALSO
   xset, getfont
  
