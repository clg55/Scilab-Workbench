
disp(1)                        Scilab Function                        disp(1)
NAME
  disp - displays variables

CALLING SEQUENCE
  disp(x1,[x2,...xn])

DESCRIPTION
  displays xi with the current format. xi's are arbitrary objects (matrices
  of constants, strings, functions, lists, ...)

SEE ALSO
  write, read, print, string
