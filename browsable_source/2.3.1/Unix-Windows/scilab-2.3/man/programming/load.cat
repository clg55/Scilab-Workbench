
load(1)                        Scilab Function                        load(1)
NAME
  load - load saved variable

CALLING SEQUENCE
  load('file-name' [x1,..])

PARAMETERS

  file-name : character string

  xi        : arbitrary Scilab variable(s)

DESCRIPTION
  load('file-name') loads the variables saved in file 'file-name'.
  load('file-name',x,y,...,z) loads only variables x,y,...,z of file 'file-
  name'. Note that functions defined in ASCII files are loaded by getf.

SEE ALSO
  save, getf
