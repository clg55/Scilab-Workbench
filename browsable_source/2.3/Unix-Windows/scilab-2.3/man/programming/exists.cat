
exists(1)                      Scilab Function                      exists(1)
NAME
  exists - checks variable  existence

CALLING SEQUENCE
  exists('variable-name')

DESCRIPTION
  returns 1 if the variable 'variable-name' exists and 0 otherwise. Caveats:
  a function which uses exists may return a result which depends on the
  environment!

SEE ALSO
  isdef, whereis, type, typeof, macrovar
