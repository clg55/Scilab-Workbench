
return(1)                      Scilab Function                      return(1)
NAME
  return - return or resume

DESCRIPTION
  In a function return stops the execution of the function.  In pause mode,
  it allows to return to lower levels.

  resume is equivalent to return.

SEE ALSO
  abort, break, pause
