
erfcx(3)                       Scilab Function                       erfcx(3)
NAME
  erfcx -  scaled complementary error function.

CALING SEQUENCE
   y = erfcx(x)

PARAMETERS

  x    : real vector

  y    : real vector (of same size)

DESCRIPTION
   erfcx computes the  scaled complementary error function:

          y = exp(x^2) * erfc(x) ~ (1/sqrt(pi)) * 1/x for large x.

SEE ALSO
  erf, erfc, calerf
