
derivat(8)                     Scilab Function                     derivat(8)
NAME
  derivat - rational matrix derivative

CALLING SEQUENCE
  pd=derivat(p)

PARAMETERS

  p    : polynomial or rational matrix

DESCRIPTION
  computes the derivative of the polynomial or rational function matrix w.r.t
  the dummy variable.

EXAMPLE
  s=poly(0,'s');
  derivat(1/s)  // -1/s^2;
