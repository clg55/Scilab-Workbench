
funptr(G)                      Scilab Function                      funptr(G)
NAME
  funptr - coding of  primitives ( wizard stuff )

DESCRIPTION
  Utility function (for experts only) funptr('name') returns 100*fun + fin
  where (fun,fin) is the internal coding of the primitive 'name' .  fun is
  the interface number and fin the routine number

SEE ALSO
  clearfun, newfun
